'use client';import {useState} from 'react';
export function IncidentCenter({rows}:{rows:any[]}){const[data,setData]=useState(rows);async function patch(id:string,status:string){const r=await fetch(`/api/flight-ops/incidents/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({status})});const j=await r.json();if(j.success)setData(data.map(x=>x.id===id?{...x,...j.row}:x));}return <div className="card"><div className="opsHead"><div><h2>Alertes opérationnelles</h2><p className="muted">Retards importants, annulations et déroutements détectés par Flight Ops.</p></div><span className="badge warn">{data.filter(x=>x.status!=='resolved').length} ouvertes</span></div><div className="incidentList">{data.map(i=><div className={`incident ${i.severity==='critical'?'critical':''}`} key={i.id}><div><div className="incidentTitle">{i.title}</div><div className="muted">{i.message}</div><div className="smallText">{new Date(i.createdAt).toLocaleString('fr-FR')} · {i.tracking?.clientPhone||i.tracking?.ticketReference||'sans contact'}</div></div><div className="inlineActions"><span className={`badge ${i.severity==='critical'?'badBadge':'warn'}`}>{i.severity}</span>{i.status==='open'&&<button className="btn" onClick={()=>patch(i.id,'acknowledged')}>Acquitter</button>}{i.status!=='resolved'&&<button className="btn primary" onClick={()=>patch(i.id,'resolved')}>Résoudre</button>}</div></div>)}{!data.length&&<div className="emptyState">Aucune alerte opérationnelle ouverte.</div>}</div></div>}

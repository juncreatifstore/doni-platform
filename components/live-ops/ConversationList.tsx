import Link from 'next/link';
type Row={id:string;waId:string;country:string|null;status:string;currentSegment:string;language:string|null;agentRequired:boolean;lastMessageAt:Date|null;updatedAt:Date;assignedAgent:{id:string;username:string;fullName:string|null}|null;messageCount:number;heldMinutes:number|null};
export function ConversationList({rows}:{rows:Row[]}){return <div className="card"><div className="liveOpsHeader"><div><h2>Conversations en direct</h2><p className="muted">Priorité aux dossiers nécessitant un agent et aux conversations récemment actives.</p></div><span className="badge ok">{rows.length} ouvertes</span></div><div className="tableWrap"><table className="table"><thead><tr><th>Client</th><th>État</th><th>Étape</th><th>Agent</th><th>Messages</th><th>Dernière activité</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><strong>{r.waId}</strong><div className="muted smallText">{r.country||'—'} · {(r.language||'—').toUpperCase()}</div></td><td><div className="statusStack"><span className={`badge ${r.status==='AGENT_HOLD'?'warn':'ok'}`}>{r.status}</span>{r.agentRequired&&<span className="badge badBadge">Agent requis</span>}</div></td><td><code>{r.currentSegment}</code></td><td>{r.assignedAgent?<><strong>{r.assignedAgent.fullName||r.assignedAgent.username}</strong>{r.heldMinutes!==null&&<div className="muted smallText">{r.heldMinutes} min</div>}</>:'DONI'}</td><td>{r.messageCount}</td><td>{r.lastMessageAt?new Date(r.lastMessageAt).toLocaleString('fr-FR'):'—'}</td><td><Link className="btn primary" href={`/live-ops/${r.id}`}>Ouvrir</Link></td></tr>)}</tbody></table>{rows.length===0&&<div className="emptyState">Aucune conversation active.</div>}</div></div>}

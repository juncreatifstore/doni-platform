'use client';
import { FormEvent, useEffect, useState } from 'react';
type U={id:string;username:string;fullName:string|null;email:string|null;role:string;active:boolean;lastLoginAt:string|null};
export function UserAdmin(){const [users,setUsers]=useState<U[]>([]);const [msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/users',{cache:'no-store'});const j=await r.json();if(r.ok)setUsers(j.users)}useEffect(()=>{load()},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const fd=new FormData(e.currentTarget);const r=await fetch('/api/admin/users',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(fd.entries()))});const j=await r.json();setMsg(r.ok?'Utilisateur créé.':`Erreur: ${j.error}`);if(r.ok){e.currentTarget.reset();await load();}}async function toggle(u:U){await fetch(`/api/admin/users/${u.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({active:!u.active})});await load();}return <><div className="card"><h2>Créer un utilisateur</h2><form className="userForm" onSubmit={create}><input name="username" placeholder="username" required/><input name="fullName" placeholder="Nom complet"/><input name="email" type="email" placeholder="Email"/><input name="password" type="password" minLength={10} placeholder="Mot de passe (10+)" required/><select name="role" defaultValue="AGENT"><option>AGENT</option><option>ADMIN</option><option>SUPER_ADMIN</option></select><button className="btn primary">Créer</button></form>{msg&&<p className="formMsg">{msg}</p>}</div><div className="card"><h2>Utilisateurs</h2><div className="tableWrap"><table className="table"><thead><tr><th>Utilisateur</th><th>Nom</th><th>Rôle</th><th>Statut</th><th>Dernière connexion</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.username}</td><td>{u.fullName||'—'}</td><td>{u.role}</td><td><span className={`badge ${u.active?'ok':'warn'}`}>{u.active?'Actif':'Désactivé'}</span></td><td>{u.lastLoginAt?new Date(u.lastLoginAt).toLocaleString():'—'}</td><td><button className="btn" onClick={()=>toggle(u)}>{u.active?'Désactiver':'Activer'}</button></td></tr>)}</tbody></table></div></div></>}

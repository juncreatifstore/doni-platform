'use client';
import {useEffect,useState} from 'react';
type Member={id:string;username:string;fullName:string|null;email:string|null;role:string;jobRole:string|null;jobRoleLabel:string|null;workload:{openLeads:number;activeCampaigns:number;studioOpen:number;reviews:number;partnerOpen:number;openTasks:number;total:number}};
const roles=[['MARKETING_MANAGER','Responsable Marketing'],['COMMUNITY_MANAGER','Community Manager'],['CONTENT_CREATOR','Créateur de contenu'],['ADS_MANAGER','Chargé Ads'],['PARTNERSHIPS_MANAGER','Partenariats & Terrain']];
export function MarketingTeam({canManage}:{canManage:boolean}){const[team,setTeam]=useState<Member[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){setLoading(true);const r=await fetch('/api/marketing/team',{cache:'no-store'});const j=await r.json();if(j.success)setTeam(j.team||[]);else setError(j.error||'Erreur');setLoading(false);}useEffect(()=>{load()},[]);async function setRole(userId:string,jobRole:string|null){const r=await fetch('/api/marketing/team',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({userId,jobRole})});const j=await r.json();if(j.success)load();else setError(j.error||'Mise à jour impossible');}if(loading)return <div className="card">Chargement de l’équipe…</div>;return <section className="marketingTeam"><div className="teamHero"><div><span className="workspaceKicker">Organisation & capacité</span><h2>Équipe Marketing</h2><p>Répartir clairement les responsabilités sans modifier les rôles de sécurité DONI.</p></div><div className="teamCount"><strong>{team.length}</strong><span>membres Marketing</span></div></div>{error?<div className="card danger">{error}</div>:null}<div className="teamRoleLegend">{roles.map(([v,l])=><div className="card" key={v}><strong>{l}</strong><span>{team.filter(x=>x.jobRole===v).length} personne(s)</span></div>)}</div><div className="teamGrid">{team.map(m=><article className="card teamMember" key={m.id}><div className="teamMemberHead"><div><strong>{m.fullName||m.username}</strong><span>{m.email||m.username}</span></div><span className="badge ok">{m.jobRoleLabel||'Fonction à définir'}</span></div><label>Fonction Marketing<select value={m.jobRole||''} disabled={!canManage} onChange={e=>setRole(m.id,e.target.value||null)}><option value="">Non définie</option>{roles.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><div className="workloadTotal"><span>Charge opérationnelle</span><strong>{m.workload.total}</strong></div><div className="workloadGrid"><span>Leads <b>{m.workload.openLeads}</b></span><span>Campagnes <b>{m.workload.activeCampaigns}</b></span><span>Studio <b>{m.workload.studioOpen}</b></span><span>Validations <b>{m.workload.reviews}</b></span><span>Partenariats <b>{m.workload.partnerOpen}</b></span><span>Tâches <b>{m.workload.openTasks}</b></span></div></article>)}{!team.length?<div className="card">Aucun utilisateur n’est actuellement rattaché au département Marketing.</div>:null}</div>{!canManage?<p className="muted">La modification des fonctions Marketing est réservée aux Admin/Super Admin.</p>:null}</section>}

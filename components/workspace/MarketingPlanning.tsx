'use client';
import {useEffect,useMemo,useState} from 'react';
type Plan={id:string;period:string;periodKey:string;title:string;focus:string;theme?:string|null;objectives:string[];budget?:number|null;currency?:string|null;specialAction?:string|null;trainingTopic?:string|null;reportNotes?:string|null;status:string;updatedAt:string};
const periods=[['DAY','Jour'],['WEEK','Semaine'],['MONTH','Mois'],['YEAR','Année']];
const statuses=[['DRAFT','Brouillon'],['ACTIVE','Actif'],['REVIEW','À revoir'],['COMPLETED','Terminé']];
export function MarketingPlanning(){const[plans,setPlans]=useState<Plan[]>([]),[period,setPeriod]=useState('MONTH'),[show,setShow]=useState(false),[form,setForm]=useState<any>({period:'MONTH',periodKey:'',title:'',focus:'',theme:'',objectives:'',budget:0,currency:'USD',specialAction:'',trainingTopic:'',status:'DRAFT'});async function load(){const r=await fetch('/api/marketing/planning',{cache:'no-store'});const j=await r.json();if(j.success)setPlans(j.plans||[])}useEffect(()=>{load()},[]);async function create(){const r=await fetch('/api/marketing/planning',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...form,objectives:String(form.objectives||'').split('\n').map((x:string)=>x.trim()).filter(Boolean)})});const j=await r.json();if(j.success){setShow(false);setForm({period:'MONTH',periodKey:'',title:'',focus:'',theme:'',objectives:'',budget:0,currency:'USD',specialAction:'',trainingTopic:'',status:'DRAFT'});load()}}async function patch(id:string,data:any){const r=await fetch(`/api/marketing/planning/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(j.success)setPlans(v=>v.map(x=>x.id===id?j.plan:x))}const visible=useMemo(()=>plans.filter(x=>x.period===period),[plans,period]);return <section className="mkPlanning"><div className="mkPlanningHero"><div><span className="workspaceKicker">Cadence marketing</span><h2>Planification & rapports</h2><p>Piloter le jour, la semaine, le mois et l'année avec des objectifs explicites et un bilan séparé.</p></div><button className="btn primary" onClick={()=>setShow(!show)}>+ Nouveau plan</button></div><div className="mkPeriodTabs">{periods.map(([v,l])=><button key={v} className={period===v?'active':''} onClick={()=>setPeriod(v)}>{l}<strong>{plans.filter(x=>x.period===v).length}</strong></button>)}</div>{show?<div className="card mkPlanForm"><div className="formGrid"><label>Période<select value={form.period} onChange={e=>setForm({...form,period:e.target.value})}>{periods.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label>Clé période<input value={form.periodKey} onChange={e=>setForm({...form,periodKey:e.target.value})} placeholder="2026-08, 2026-W34, 2026..."/></label><label>Titre<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Focus principal<input value={form.focus} onChange={e=>setForm({...form,focus:e.target.value})}/></label><label>Thème<input value={form.theme} onChange={e=>setForm({...form,theme:e.target.value})} placeholder="Vacances, étudiants, Bogotá..."/></label><label>Budget<input type="number" min="0" value={form.budget} onChange={e=>setForm({...form,budget:Number(e.target.value)})}/></label><label>Devise<input value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}/></label><label>Action spéciale<input value={form.specialAction} onChange={e=>setForm({...form,specialAction:e.target.value})}/></label><label>Formation équipe<input value={form.trainingTopic} onChange={e=>setForm({...form,trainingTopic:e.target.value})}/></label></div><label>Objectifs · un par ligne<textarea rows={5} value={form.objectives} onChange={e=>setForm({...form,objectives:e.target.value})}/></label><button className="btn primary" disabled={!form.periodKey.trim()||!form.title.trim()||!form.focus.trim()} onClick={create}>Créer le plan</button></div>:null}<div className="mkPlanList">{visible.map(p=><article key={p.id} className="card mkPlanCard"><div className="mkPlanTop"><div><span className="badge ok">{p.periodKey}</span><h3>{p.title}</h3><p>{p.focus}</p></div><select value={p.status} onChange={e=>patch(p.id,{status:e.target.value})}>{statuses.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div><div className="mkPlanFacts"><div><span>Thème</span><strong>{p.theme||'À définir'}</strong></div><div><span>Budget</span><strong>{p.budget==null?'—':`${p.budget.toLocaleString('fr-FR')} ${p.currency||''}`}</strong></div><div><span>Action spéciale</span><strong>{p.specialAction||'Aucune'}</strong></div><div><span>Formation</span><strong>{p.trainingTopic||'À définir'}</strong></div></div><div className="mkObjectives"><span>Objectifs</span>{p.objectives.length?p.objectives.map((o,i)=><div key={i}>✓ {o}</div>):<div className="muted">Aucun objectif saisi.</div>}</div><label className="mkReportLabel">Bilan / rapport<textarea rows={4} value={p.reportNotes||''} onChange={e=>setPlans(v=>v.map(x=>x.id===p.id?{...x,reportNotes:e.target.value}:x))} onBlur={e=>patch(p.id,{reportNotes:e.target.value||null})} placeholder="Résultats, écarts, leçons, décisions pour la période suivante…"/></label></article>)}{!visible.length?<div className="card portfolioEmpty">Aucun plan pour cette période.</div>:null}</div></section>}

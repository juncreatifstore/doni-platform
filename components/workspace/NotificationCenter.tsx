'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
type N={id:string;title:string;message:string;severity:'INFO'|'WARNING'|'CRITICAL';href?:string|null;readAt?:string|null;createdAt:string};
export function NotificationCenter(){const[rows,setRows]=useState<N[]>([]),[loading,setLoading]=useState(true),[unread,setUnread]=useState(0);async function load(){setLoading(true);const r=await fetch('/api/workspace/notifications',{cache:'no-store'});const j=await r.json();if(j.success){setRows(j.notifications||[]);setUnread(Number(j.unread||0))}setLoading(false)}useEffect(()=>{load()},[]);async function mark(id:string){await fetch(`/api/workspace/notifications/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({read:true})});await load()}async function markAll(){await fetch('/api/workspace/notifications',{method:'PATCH'});await load()}return <section className="notificationCenter"><div className="notificationHeader"><div><span className="workspaceKicker">Centre interne</span><h2>Notifications DONI</h2><p>{unread} notification(s) non lue(s)</p></div><button className="btn" onClick={markAll} disabled={!unread}>Tout marquer comme lu</button></div>{loading?<div className="card muted">Chargement…</div>:rows.length?<div className="notificationList">{rows.map(n=><article key={n.id} className={`notificationItem severity-${n.severity.toLowerCase()} ${n.readAt?'read':'unread'}`}><div className="notificationDot"/><div className="notificationCopy"><div className="notificationTitleRow"><strong>{n.title}</strong><span>{n.severity}</span></div><p>{n.message}</p><small>{new Date(n.createdAt).toLocaleString('fr-FR')}</small></div><div className="notificationActions">{n.href?<Link className="btn" href={n.href} onClick={()=>mark(n.id)}>Ouvrir</Link>:null}{!n.readAt?<button className="btn" onClick={()=>mark(n.id)}>Lu</button>:null}</div></article>)}</div>:<div className="card muted">Aucune notification interne.</div>}</section>}

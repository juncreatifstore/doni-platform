'use client';
import {useMemo,useState} from 'react';
type Money={currency:string;amount:number};
type Row={key:string;name:string;phone?:string|null;email?:string|null;sources:string[];leadStatuses:string[];destinations:string[];sales:number;revenueByCurrency:Money[];reviews:number;averageRating:number|null;loyaltyTier?:string|null;loyaltyStatus?:string|null;referralsGiven:number;referralsGivenPaid:number;referralsReceived:number;lastActivityAt:string;links:{leads:boolean;loyalty:boolean;reviews:boolean;referrals:boolean}};
type Workspace={items:Row[];summary:{profiles:number;withSales:number;withLoyalty:number;ambassadors:number;unmatchedReviews:number}};
const tierLabels:Record<string,string>={NEW:'Nouveau',ACTIVE:'Actif',LOYAL:'Fidèle',VIP:'VIP',REACTIVATE:'À réactiver'};
export function MarketingLifecycle({workspace}:{workspace:Workspace}){const[q,setQ]=useState(''),[filter,setFilter]=useState('ALL');const visible=useMemo(()=>workspace.items.filter(x=>{const hay=`${x.name} ${x.phone||''} ${x.email||''} ${x.sources.join(' ')} ${x.destinations.join(' ')}`.toLowerCase();if(q&&!hay.includes(q.toLowerCase()))return false;if(filter==='SALES'&&x.sales===0)return false;if(filter==='LOYALTY'&&!x.links.loyalty)return false;if(filter==='AMBASSADOR'&&x.referralsGiven===0)return false;if(filter==='REACTIVATE'&&x.loyaltyTier!=='REACTIVATE')return false;return true}),[workspace.items,q,filter]);return <section className="lifecycleWorkspace"><div className="lifecycleKpis"><div className="card"><span>Profils reliés</span><strong>{workspace.summary.profiles}</strong></div><div className="card"><span>Avec ventes</span><strong>{workspace.summary.withSales}</strong></div><div className="card"><span>Fidélité active</span><strong>{workspace.summary.withLoyalty}</strong></div><div className="card"><span>Ambassadeurs</span><strong>{workspace.summary.ambassadors}</strong></div><div className={workspace.summary.unmatchedReviews?'card warning':'card'}><span>Avis non reliés</span><strong>{workspace.summary.unmatchedReviews}</strong></div></div><div className="marketingToolbar"><div><h2>Cycle client unifié</h2><p>Acquisition → vente → réputation → fidélité → recommandation, sans fusion sur le nom seul.</p></div><div className="marketingToolbarActions"><input placeholder="Client, téléphone, email, destination..." value={q} onChange={e=>setQ(e.target.value)}/><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="ALL">Tous les profils</option><option value="SALES">Avec vente</option><option value="LOYALTY">Avec fidélité</option><option value="AMBASSADOR">Ambassadeurs</option><option value="REACTIVATE">À réactiver</option></select></div></div><div className="lifecycleGrid">{visible.map(x=><article className="card lifecycleCard" key={x.key}><div className="lifecycleHead"><div><h3>{x.name}</h3><p className="muted">{x.phone||x.email||'Identifiant marketing isolé'}</p></div>{x.loyaltyTier?<span className={`badge ${x.loyaltyTier==='VIP'?'ok':x.loyaltyTier==='REACTIVATE'?'warn':''}`}>{tierLabels[x.loyaltyTier]||x.loyaltyTier}</span>:null}</div><div className="lifecycleSteps"><span className={x.links.leads?'done':''}>Prospect</span><span className={x.sales>0?'done':''}>Vente</span><span className={x.links.reviews?'done':''}>Avis</span><span className={x.links.loyalty?'done':''}>Fidélité</span><span className={x.links.referrals?'done':''}>Parrainage</span></div><div className="lifecycleStats"><div><strong>{x.sales}</strong><span>ventes attribuées</span></div><div><strong>{x.reviews?`${x.averageRating}/5`:'—'}</strong><span>{x.reviews} avis liés</span></div><div><strong>{x.referralsGiven}</strong><span>parrainages donnés</span></div><div><strong>{x.referralsReceived}</strong><span>fois parrainé</span></div></div>{x.revenueByCurrency.length?<div className="lifecycleMoney">{x.revenueByCurrency.map(v=><span key={v.currency}>{v.amount.toFixed(2)} {v.currency}</span>)}</div>:null}{x.sources.length?<p><strong>Acquisition :</strong> {x.sources.join(', ')}</p>:null}{x.destinations.length?<p><strong>Destinations :</strong> {x.destinations.join(', ')}</p>:null}{x.leadStatuses.length?<p><strong>Statut prospect :</strong> {x.leadStatuses.join(', ')}</p>:null}<div className="lifecycleFooter"><small>Dernière activité · {new Date(x.lastActivityAt).toLocaleString('fr-FR')}</small><div className="lifecycleLinks">{x.links.leads?<a href="/marketing/leads">Leads</a>:null}{x.links.loyalty?<a href="/marketing/loyalty">Fidélité</a>:null}{x.links.reviews?<a href="/marketing/reviews">Avis</a>:null}{x.links.referrals?<a href="/marketing/referrals">Parrainage</a>:null}</div></div></article>)}{!visible.length?<div className="card portfolioEmpty">Aucun profil dans cette vue.</div>:null}</div><div className="card lifecycleNote"><strong>Règle de rapprochement</strong><p>DONI utilise téléphone/email normalisés, ou une référence de réservation reliée à une vente attribuée. Un nom identique ne suffit jamais pour fusionner deux profils.</p></div></section>}

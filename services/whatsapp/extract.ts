export interface WhatsAppInbound{waId:string;text:string;messageId:string|null;displayName:string|null;raw:unknown;}
export function extractWhatsAppMessages(payload:any):WhatsAppInbound[]{const out:WhatsAppInbound[]=[];for(const entry of payload?.entry??[])for(const change of entry?.changes??[]){const value=change?.value??{};const contacts=new Map<string,string>();for(const c of value?.contacts??[])if(c?.wa_id)contacts.set(String(c.wa_id),String(c?.profile?.name||''));for(const msg of value?.messages??[]){let text='';if(msg?.type==='text')text=msg.text?.body??'';else if(msg?.type==='button')text=msg.button?.text??msg.button?.payload??'';else if(msg?.type==='interactive')text=msg.interactive?.button_reply?.title??msg.interactive?.list_reply?.title??'';else if(msg?.type==='image')text='[image]';if(msg?.from&&text)out.push({waId:String(msg.from),text:String(text),messageId:msg.id??null,displayName:contacts.get(String(msg.from))||null,raw:msg});}}return out;}

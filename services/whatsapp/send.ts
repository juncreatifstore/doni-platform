import {getSetting} from '@/lib/settings/service';
export async function sendWhatsAppText(to:string,body:string){const token=await getSetting<string>('whatsapp.access_token');const phoneId=await getSetting<string>('whatsapp.phone_number_id');const enabled=await getSetting<boolean>('whatsapp.enabled');if(!enabled)return {sent:false,dryRun:true,to,body};if(!token||!phoneId)throw new Error('WhatsApp credentials missing');const res=await fetch(`https://graph.facebook.com/v23.0/${phoneId}/messages`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({messaging_product:'whatsapp',to,type:'text',text:{body}})});const json=await res.json();if(!res.ok)throw new Error(`WhatsApp send failed: ${res.status} ${JSON.stringify(json)}`);return {sent:true,dryRun:false,response:json};}

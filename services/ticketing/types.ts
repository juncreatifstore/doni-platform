export type TicketPassenger = { firstName?:string; lastName?:string; type?:string; documentNumber?:string; nationality?:string; [key:string]:unknown };
export type TicketSegment = { airline?:string; airlineName?:string; flightNumber?:string; origin?:string; destination?:string; departureAt?:string; arrivalAt?:string; cabin?:string; pnr?:string; bookingReference?:string; [key:string]:unknown };
export type TicketPayload = { passengers?:TicketPassenger[]; travelers?:any[]; segments?:TicketSegment[]; contact?:{email?:string;phone?:string}; total?:number|string; amount?:number|string; currency?:string; provider?:string; offerId?:string; offer_id?:string; [key:string]:unknown };
export function normalizeTicketPayload(v:unknown):TicketPayload{const p=(v&&typeof v==='object'?v:{}) as any;const srcP=Array.isArray(p.passengers)?p.passengers:Array.isArray(p.travelers)?p.travelers:[];const passengers=srcP.map((x:any)=>({firstName:x.firstName??x.first_name??'',lastName:x.lastName??x.last_name??'',type:x.type??x.passenger_type??'',documentNumber:x.documentNumber??x.passport_number??x.document_number??'',nationality:x.nationality??''}));const srcS=Array.isArray(p.segments)?p.segments:[];const segments=srcS.map((x:any)=>({airline:x.airline??x.airline_code??'',airlineName:x.airlineName??x.airline_name??'',flightNumber:x.flightNumber??x.flight_number??'',origin:x.origin??'',destination:x.destination??'',departureAt:x.departureAt??x.departure_at??'',arrivalAt:x.arrivalAt??x.arrival_at??'',cabin:x.cabin??x.cabin_class??'Economy',pnr:x.pnr??x.bookingReference??x.booking_reference??x.locator??'',bookingReference:x.bookingReference??x.booking_reference??x.pnr??x.locator??''}));return {...p,passengers,segments,total:p.total??p.amount,offerId:p.offerId??p.offer_id};}

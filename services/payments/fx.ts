import {getSetting} from '@/lib/settings/service';
export interface FxResult{amount:number;from:string;to:string;rate:number;source:string;timestamp:string}
function envRate(from:string,to:string){const v=process.env[`FX_${from}_${to}`];const n=Number(v);return Number.isFinite(n)&&n>0?n:null}
export async function convertCurrency(amount:number,fromRaw:string,toRaw:string):Promise<FxResult|null>{const from=fromRaw.toUpperCase(),to=toRaw.toUpperCase();if(from===to)return{amount:Math.round(amount*100)/100,from,to,rate:1,source:'same_currency',timestamp:new Date().toISOString()};const manual=envRate(from,to);if(manual)return{amount:Math.round(amount*manual*100)/100,from,to,rate:manual,source:'env_manual',timestamp:new Date().toISOString()};const key=await getSetting<string>('fx.openexchangerates_app_id');if(!key)return null;try{const r=await fetch(`https://openexchangerates.org/api/latest.json?app_id=${encodeURIComponent(key)}`,{headers:{Accept:'application/json'}});const j=await r.json();if(!r.ok||!j?.rates)return null;const usdFrom=from==='USD'?1:Number(j.rates[from]);const usdTo=to==='USD'?1:Number(j.rates[to]);if(!usdFrom||!usdTo)return null;const rate=usdTo/usdFrom;return{amount:Math.round(amount*rate*100)/100,from,to,rate,source:'OpenExchangeRates',timestamp:new Date().toISOString()};}catch{return null}}

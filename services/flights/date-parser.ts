import type {DoniLanguage} from '../conversation/types';
const months:Record<string,number>={janvier:1,january:1,enero:1,janvye:1,'février':2,fevrier:2,february:2,febrero:2,fevriye:2,mars:3,march:3,marzo:3,mas:3,avril:4,april:4,abril:4,mai:5,may:5,mayo:5,me:5,juin:6,june:6,junio:6,jen:6,juillet:7,july:7,julio:7,'jiyè':7,jiye:7,'août':8,aout:8,august:8,agosto:8,out:8,septembre:9,september:9,septiembre:9,septanm:9,octobre:10,october:10,octubre:10,'oktòb':10,oktob:10,novembre:11,november:11,noviembre:11,novanm:11,'décembre':12,decembre:12,december:12,diciembre:12,desanm:12};
const pad=(n:number)=>String(n).padStart(2,'0'); const iso=(y:number,m:number,d:number)=>`${y}-${pad(m)}-${pad(d)}`;
function valid(y:number,m:number,d:number){const dt=new Date(Date.UTC(y,m-1,d));return dt.getUTCFullYear()===y&&dt.getUTCMonth()===m-1&&dt.getUTCDate()===d;}
export function todayIso(){const n=new Date();return iso(n.getFullYear(),n.getMonth()+1,n.getDate());}
export function parseFlightDate(text:string,lang:DoniLanguage|null){const t=text.trim().toLocaleLowerCase();const rel:Record<string,number>={'today':0,"aujourd'hui":0,'hoy':0,'jodi a':0,'tomorrow':1,'demain':1,'mañana':1,'demen':1};if(t in rel){const d=new Date();d.setDate(d.getDate()+rel[t]);return iso(d.getFullYear(),d.getMonth()+1,d.getDate());}const m=t.match(/(\d{1,2})[\/\-.](\d{1,2})[\/\-.](\d{2,4})/);if(m){let a=+m[1],b=+m[2],y=+m[3];if(y<100)y+=2000;let day=a,mo=b;if(b>12&&a<=12){day=b;mo=a;}else if(a<=12&&b<=12&&lang==='en'){day=b;mo=a;}if(valid(y,mo,day))return iso(y,mo,day);}for(const [name,mo] of Object.entries(months)){const re=new RegExp(`(\\d{1,2})\\s*(?:de\\s+)?${name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}\\s*(\\d{4})?`,'u');const hit=t.match(re);if(hit){const day=+hit[1];let y=hit[2]?+hit[2]:new Date().getFullYear();if(!hit[2]&&new Date(`${iso(y,mo,day)}T00:00:00`).getTime()<new Date(`${todayIso()}T00:00:00`).getTime())y++;if(valid(y,mo,day))return iso(y,mo,day);}}return null;}
export function isPastDate(value:string){return value<todayIso();}

import {db} from '@/lib/db';
export async function flightOpsSummary(){const now=new Date();const [active,due,delayed,pendingAlerts,failedAlerts,checkins]=await Promise.all([(db as any).flightTracking.count({where:{active:true}}),(db as any).flightTracking.count({where:{active:true,nextCheckAt:{lte:now}}}),(db as any).flightTracking.count({where:{active:true,flightStatus:'delayed'}}),(db as any).flightAlert.count({where:{status:'pending'}}),(db as any).flightAlert.count({where:{status:'failed'}}),(db as any).checkinService.count({where:{status:{in:['offered','accepted','payment_pending']}}})]);const rows=await (db as any).flightTracking.findMany({where:{active:true},orderBy:{scheduledDeparture:'asc'},take:50});return {active,due,delayed,pendingAlerts,failedAlerts,checkins,rows};}

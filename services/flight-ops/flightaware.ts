import {getSetting} from '@/lib/settings/service';
export type FlightStatus={ident?:string;status:string;scheduledDeparture?:string|null;estimatedDeparture?:string|null;actualDeparture?:string|null;scheduledArrival?:string|null;estimatedArrival?:string|null;actualArrival?:string|null;delayMinutes:number;gate?:string|null;terminal?:string|null;origin?:string|null;destination?:string|null;raw:any};
function best(list:any[],date:string){if(!list.length)return null;const target=new Date(`${date}T12:00:00Z`).getTime();return [...list].sort((a,b)=>Math.abs(new Date(a.scheduled_out||0).getTime()-target)-Math.abs(new Date(b.scheduled_out||0).getTime()-target))[0];}
const IATA_TO_ICAO:Record<string,string>={AV:'AVA'};
function providerIdent(code:string,num:string){const c=String(code||'').trim().toUpperCase();const raw=String(num||'').trim().toUpperCase().replace(/^([A-Z]{2,3})/,'');const numeric=/^0*\d+$/.test(raw)?String(Number(raw)):raw;return `${IATA_TO_ICAO[c]||c}${numeric}`;}
export async function queryFlightAwareStatus(code:string,num:string,date:string){const key=await getSetting<string>('tracking.flightaware_api_key');if(!key)return {ok:false,error:'flightaware_no_api_key'};const ts=new Date(`${date}T00:00:00Z`).getTime();const q=new URLSearchParams({max_pages:'1',start:new Date(ts-12*3600000).toISOString().replace('.000',''),end:new Date(ts+48*3600000).toISOString().replace('.000','')});const ident=providerIdent(code,num);try{const r=await fetch(`https://aeroapi.flightaware.com/aeroapi/flights/${encodeURIComponent(ident)}?${q}`,{headers:{'x-apikey':key,Accept:'application/json'},signal:AbortSignal.timeout(20000)});const j:any=await r.json();if(!r.ok)return {ok:false,error:j?.title||j?.detail||`HTTP ${r.status}`,status:r.status,ident};const f=best(j?.flights||[],date);if(!f)return {ok:false,error:'no_matching_flight',ident};const delay=Math.round(Number(f.departure_delay??f.arrival_delay??0)/60);let status='scheduled';if(f.cancelled)status='cancelled';else if(f.diverted)status='diverted';else if(f.actual_in)status='landed';else if(f.actual_out)status='in_air';else if(delay>=15)status='delayed';const normalized:FlightStatus={ident:f.ident,status,scheduledDeparture:f.scheduled_out,estimatedDeparture:f.estimated_out,actualDeparture:f.actual_out,scheduledArrival:f.scheduled_in,estimatedArrival:f.estimated_in,actualArrival:f.actual_in,delayMinutes:delay,gate:f.gate_origin||f.gate_destination||null,terminal:f.terminal_origin||f.terminal_destination||null,origin:f?.origin?.code_iata||f?.origin?.code||null,destination:f?.destination?.code_iata||f?.destination?.code||null,raw:f};return {ok:true,status:r.status,normalized,ident};}catch(e){return {ok:false,error:e instanceof Error?e.message:'flightaware_failed',ident};}}
export async function getFlightAwareStatus(code:string,num:string,date:string){const enabled=await getSetting<boolean>('tracking.enabled');if(!enabled)return {ok:false,error:'tracking_disabled'};return queryFlightAwareStatus(code,num,date);}

import {db} from '@/lib/db';import {audit} from '@/lib/audit';import type {SafeUser} from '@/lib/auth/session';
const allowed=new Set(['offered','accepted','payment_pending','completed','declined']);
export async function listCheckins(){return (db as any).checkinService.findMany({include:{tracking:true},orderBy:{createdAt:'desc'},take:200});}
export async function updateCheckin(id:string,input:any,user:SafeUser){const current=await (db as any).checkinService.findUnique({where:{id}});if(!current)throw new Error('checkin_not_found');const status=String(input.status??current.status);if(!allowed.has(status))throw new Error('invalid_status');const data:any={status};if('seatPreference'in input)data.seatPreference=String(input.seatPreference||'').trim()||null;if('paymentStatus'in input)data.paymentStatus=String(input.paymentStatus||'').trim()||null;if('notes'in input)data.notes=String(input.notes||'').trim()||null;if(status==='accepted'&&!data.paymentStatus)data.paymentStatus='pending';if(status==='completed'){data.completedBy=user.id;data.completedAt=new Date();}const row=await (db as any).checkinService.update({where:{id},data});await audit({userId:user.id,action:'checkin.update',entity:'CheckinService',entityId:id,metadata:{from:current.status,to:status}});return row;}

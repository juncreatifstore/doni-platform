import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import type {FlightOffer} from '../flights/types';import {markMarketingFlightSearchSelected} from '@/lib/workspace/marketing-search-intelligence';
export class SelectionSegment extends BaseSegment{readonly id='segment_selection';async handle(s:ConversationSession,text:string){const offers=(s.state.presented_offers??[]) as FlightOffer[];const n=Number(text.trim());if(!Number.isInteger(n)||n<1||n>offers.length)return this.retry(this.t(s,{fr:`Choisissez un vol entre 1 et ${offers.length}.`,en:`Choose a flight between 1 and ${offers.length}.`,es:`Elija un vuelo entre 1 y ${offers.length}.`,ht:`Chwazi yon vòl ant 1 ak ${offers.length}.`}));const o=offers[n-1];const searchEventId=typeof s.state.search_event_id==='string'?s.state.search_event_id:null;if(searchEventId)await markMarketingFlightSearchSelected(searchEventId,o).catch(()=>null);return this.reply(this.t(s,{fr:`✅ Option ${n} sélectionnée.\n\nLa vérification du prix et la collecte des voyageurs commencent maintenant.`,en:`✅ Option ${n} selected.\n\nPrice verification and traveler collection are starting now.`,es:`✅ Opción ${n} seleccionada.\n\nLa verificación de precio y los viajeros empiezan ahora.`,ht:`✅ Opsyon ${n} chwazi.\n\nVerifikasyon pri ak enfòmasyon pasaje yo kòmanse kounye a.`}),'segment_repricing',{selected_offer:o},{normalized:o.offer_id,autoChain:true});}prompt(){return '1, 2, 3...';}}

import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import type {FlightCriteria} from '../flights/types';import {travelerPlan} from '../travelers/flow';
export class SavedPassengersSegment extends BaseSegment{readonly id='segment_saved_passengers';handle(s:ConversationSession){const plan=travelerPlan((s.state.criteria as FlightCriteria|undefined)?.passengers);const collected=Array.isArray(s.state.travelers)?s.state.travelers:[];if(collected.length>=plan.length)return this.reply(null,'segment_contact_email',null,{autoChain:true});return this.reply(null,'segment_new_passenger',{traveler_plan:plan,current_traveler_index:collected.length},{autoChain:true});}prompt(s:ConversationSession){return this.t(s,{fr:'Préparation des voyageurs…',en:'Preparing travelers…',es:'Preparando viajeros…',ht:'M ap prepare pasaje yo…'});}}

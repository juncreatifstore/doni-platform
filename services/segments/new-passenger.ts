import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';
export class NewPassengerSegment extends BaseSegment{readonly id='segment_new_passenger';handle(s:ConversationSession,text:string,raw?:any){const plan=(s.state.traveler_plan??[]) as string[];const idx=Number(s.state.current_traveler_index??0);const kind=plan[idx]??'adult';const imageId=raw?.image?.id||raw?.mediaId;if(imageId)return this.reply(null,'segment_ocr_upload',{pending_media_id:String(imageId)},{autoChain:true});if(text.trim()==='1')return this.reply(this.t(s,{fr:'📷 Envoyez maintenant une photo claire de la page d’identité du passeport.',en:'📷 Send a clear photo of the passport identity page now.',es:'📷 Envíe ahora una foto clara de la página de identidad del pasaporte.',ht:'📷 Voye yon foto klè paj idantite paspò a kounye a.'}),'segment_ocr_upload');if(text.trim()==='2')return this.reply(null,'segment_manual_passenger',{manual_field:'first_name',traveler_draft:{type:kind,source:'manual'}},{autoChain:true});return this.retry(this.t(s,{fr:`👤 Voyageur ${idx+1}/${plan.length} (${kind})\n\n1. 📷 Lire le passeport par photo\n2. ✍️ Saisir manuellement`,en:`👤 Traveler ${idx+1}/${plan.length} (${kind})\n\n1. 📷 Read passport from photo\n2. ✍️ Enter manually`,es:`👤 Viajero ${idx+1}/${plan.length} (${kind})\n\n1. 📷 Leer pasaporte por foto\n2. ✍️ Ingresar manualmente`,ht:`👤 Pasaje ${idx+1}/${plan.length} (${kind})\n\n1. 📷 Li paspò a ak foto\n2. ✍️ Antre enfòmasyon yo manyèlman`}));}prompt(s:ConversationSession){return this.handle(s,'').message??'';}}

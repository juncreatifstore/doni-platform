import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';
export class ContactPhoneSegment extends BaseSegment{readonly id='segment_contact_phone';handle(s:ConversationSession,text:string){const v=text.trim();if(!v)return this.retry(this.t(s,{fr:`📱 Numéro de téléphone ?\nRépondez *même* pour utiliser votre numéro WhatsApp (${s.waId}).`,en:`📱 Phone number?\nReply *same* to use your WhatsApp number (${s.waId}).`,es:`📱 ¿Número de teléfono?\nResponda *mismo* para usar su WhatsApp (${s.waId}).`,ht:`📱 Nimewo telefòn?\nReponn *menm* pou itilize nimewo WhatsApp ou (${s.waId}).`}));const same=/^(meme|même|same|mismo|menm)$/i.test(v);const phone=same?s.waId:v.replace(/[^\d+]/g,'');if(phone.replace(/\D/g,'').length<7)return this.retry(this.t(s,{fr:'❌ Numéro invalide.',en:'❌ Invalid number.',es:'❌ Número inválido.',ht:'❌ Nimewo a pa valid.'}));return this.reply(null,'segment_compliance',{contact_phone:phone},{autoChain:true});}prompt(s:ConversationSession){return this.handle(s,'').message??'';}}

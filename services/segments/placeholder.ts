import {BaseSegment} from './base'; import type {ConversationSession} from '../conversation/types';
export class PlaceholderSegment extends BaseSegment{constructor(public readonly id:string,private readonly label:string){super();}handle(session:ConversationSession){return this.retry(this.t(session,{fr:`⚙️ ${this.label} est en cours de migration vers DONI Vercel.`,en:`⚙️ ${this.label} is being migrated to DONI Vercel.`,es:`⚙️ ${this.label} se está migrando a DONI Vercel.`,ht:`⚙️ ${this.label} ap migre sou DONI Vercel.`}));}prompt(session:ConversationSession){return this.handle(session).message??'';}}

import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';
export class StopsSegment extends BaseSegment{readonly id='segment_stops';handle(s:ConversationSession,text:string){const t=text.trim().toLocaleLowerCase();const any=['peu importe','tout','aucun','aucune','any','whatever','cualquiera','todos','sin importar','nenpòt','nenpot','tout sa','*'].some(k=>t.includes(k));let max:number|null=null;if(!any){if(t.includes('direct')||t.includes('dirèk'))max=0;else{const m=t.match(/\d+/);if(!m||+m[0]>3)return this.retry(this.t(s,{fr:"Tapez le nombre d'escales (0 direct, 1, 2) ou tout.",en:'Type max stops (0 direct, 1, 2) or any.',es:'Escriba el máx de escalas (0 directo, 1, 2) o todos.',ht:'Tape kantite eskal (0 dirèk, 1, 2) oswa tout.'}));max=+m[0];}}return this.reply(this.t(s,{fr:'✅ Préférence escales enregistrée.',en:'✅ Stops preference saved.',es:'✅ Preferencia de escalas guardada.',ht:'✅ Preferans eskal anrejistre.'}),'segment_airline',{criteria:{max_stops:max}},{normalized:any?'any':String(max)});}prompt(s:ConversationSession){return this.t(s,{fr:"Combien d'escales maximum ?",en:'Max stops?',es:'¿Escalas máximo?',ht:'Konbyen eskal maksimòm?'});}}

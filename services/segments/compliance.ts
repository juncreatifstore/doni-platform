import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import type {FlightOffer} from '../flights/types';import type {Traveler} from '../travelers/types';import {checkCompliance,complianceMessage} from '../flights/compliance';
export class ComplianceSegment extends BaseSegment{readonly id='segment_compliance';handle(s:ConversationSession){const offer=s.state.selected_offer as FlightOffer|undefined;const travelers=(s.state.travelers??[]) as Traveler[];if(!offer||!travelers.length)return this.reply('⚠️ Données de réservation incomplètes. Un agent va vérifier le dossier.','segment_agent_escalation',{requires_human_review:true},{autoChain:true});const r=checkCompliance(offer,travelers);if(!r.ok)return this.reply(`⚠️ Vérification avant réservation\n\n${complianceMessage(r)}\n\nNous devons corriger ces informations avant le paiement.`,'segment_saved_passengers',{compliance:r,travelers:[],current_traveler_index:0},{autoChain:true});return this.reply(r.warnings.length?`⚠️ Vérification terminée avec ${r.warnings.length} avertissement(s).`:'✅ Vérification voyageurs terminée.','segment_recap',{compliance:r},{autoChain:true});}prompt(s:ConversationSession){return this.handle(s).message??'';}}

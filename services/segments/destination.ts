import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import {findAirportsByCity,formatAirportChoices} from '../flights/airport-engine';
export class DestinationSegment extends BaseSegment{readonly id='segment_destination';handle(s:ConversationSession,text:string){const q=text.trim();if(q.length<2)return this.retry(this.t(s,{fr:'Veuillez préciser la ville de destination.',en:'Please specify the destination city.',es:'Por favor especifique la ciudad de destino.',ht:'Tanpri presize vil destinasyon an.'}));const m=findAirportsByCity(q);if(!m.length)return this.retry(this.t(s,{fr:"Je n'ai pas trouvé cette ville. Reformulez ou utilisez le code aéroport.",en:"I couldn't find this city. Rephrase or use airport code.",es:'No encontré esta ciudad. Reformule o use el código de aeropuerto.',ht:'M pa jwenn vil sa. Di l yon lòt jan oswa itilize kòd ayewopò.'}));if(m.length===1){const a=m[0];return this.reply(this.t(s,{fr:`✅ Destination : *${a.city}* (${a.code})\n\n📅 Quelle est la *date de départ* ? (format: 15/06/2026 ou "15 juin")`,en:`✅ Destination: *${a.city}* (${a.code})\n\n📅 What's the *departure date*?`,es:`✅ Destino: *${a.city}* (${a.code})\n\n📅 ¿Cuál es la *fecha de salida*?`,ht:`✅ Destinasyon: *${a.city}* (${a.code})\n\n📅 Ki *dat depa* a?`}),'segment_departure_date',{criteria:{destination:a.code,destination_city:a.city}},{normalized:a.code});}return this.reply(this.t(s,{fr:'Plusieurs aéroports trouvés. Lequel ?',en:'Multiple airports found. Which one?',es:'Múltiples aeropuertos encontrados. ¿Cuál?',ht:'Plizyè ayewopò jwenn. Kilès?'})+'\n'+formatAirportChoices(m),'segment_destination_airport',{destination_candidates:m});}prompt(s:ConversationSession){return this.t(s,{fr:'Quelle est votre destination ?',en:"What's your destination?",es:'¿Cuál es su destino?',ht:'Ki destinasyon ou?'});}}

import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import {parseFlightDate} from '../flights/date-parser';import type {FlightCriteria} from '../flights/types';
export class ReturnDateSegment extends BaseSegment{readonly id='segment_return_date';handle(s:ConversationSession,text:string){const d=parseFlightDate(text,s.lockedLanguage);if(!d)return this.retry(this.t(s,{fr:'Date de retour invalide.',en:'Invalid return date.',es:'Fecha de regreso inválida.',ht:'Dat retou envalid.'}));const c=(s.state.criteria??{}) as FlightCriteria;if(c.depart_date&&d<=c.depart_date)return this.retry(this.t(s,{fr:'Le retour doit être après le départ.',en:'Return must be after departure.',es:'El regreso debe ser después de la salida.',ht:'Retou dwe apre depa.'}));const p=d.split('-').reverse().join('/');return this.reply(this.t(s,{fr:`✅ Retour : *${p}*\n\n👥 Combien de *passagers* ? Précisez adultes / enfants / bébés.`,en:`✅ Return: *${p}*\n\n👥 How many *passengers*?`,es:`✅ Regreso: *${p}*\n\n👥 ¿Cuántos *pasajeros*?`,ht:`✅ Retou: *${p}*\n\n👥 Konbyen *pasaje*?`}),'segment_passengers',{criteria:{return_date:d}},{normalized:d});}prompt(s:ConversationSession){return this.t(s,{fr:'Date de retour ?',en:'Return date?',es:'Fecha de regreso?',ht:'Dat retou?'});}}

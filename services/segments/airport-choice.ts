import {BaseSegment} from './base';import type {ConversationSession} from '../conversation/types';import type {Airport} from '../flights/types';
function choice(text:string){const m=text.match(/\d+/);return m?Number(m[0]):null;}
export class OriginAirportSegment extends BaseSegment{readonly id='segment_origin_airport';handle(s:ConversationSession,text:string){const c=(s.state.origin_candidates??[]) as Airport[];if(!c.length)return this.reply(null,'segment_origin',null,{autoChain:true});const n=choice(text);if(!n||n<1||n>c.length)return this.retry(this.t(s,{fr:"Tapez le numéro de l'aéroport (1, 2, 3...).",en:'Type the airport number (1, 2, 3...).',es:'Escriba el número del aeropuerto (1, 2, 3...).',ht:'Tape nimewo ayewopò a (1, 2, 3...).'}));const a=c[n-1];return this.reply(this.t(s,{fr:`✅ Départ : *${a.city}* (${a.code})\n\nEt votre *destination* ?`,en:`✅ Departure: *${a.city}* (${a.code})\n\nAnd your *destination*?`,es:`✅ Salida: *${a.city}* (${a.code})\n\n¿Y su *destino*?`,ht:`✅ Depa: *${a.city}* (${a.code})\n\nEpi *destinasyon* ou?`}),'segment_destination',{criteria:{origin:a.code,origin_city:a.city},origin_candidates:null},{normalized:a.code});}prompt(){return '1, 2, 3...';}}
export class DestinationAirportSegment extends BaseSegment{readonly id='segment_destination_airport';handle(s:ConversationSession,text:string){const c=(s.state.destination_candidates??[]) as Airport[];if(!c.length)return this.reply(null,'segment_destination',null,{autoChain:true});const n=choice(text);if(!n||n<1||n>c.length)return this.retry(this.t(s,{fr:'Tapez le numéro (1, 2, 3...).',en:'Type the number (1, 2, 3...).',es:'Escriba el número (1, 2, 3...).',ht:'Tape nimewo a (1, 2, 3...).'}));const a=c[n-1];return this.reply(this.t(s,{fr:`✅ Destination : *${a.city}* (${a.code})\n\n📅 *Date de départ* ?`,en:`✅ Destination: *${a.city}* (${a.code})\n\n📅 *Departure date*?`,es:`✅ Destino: *${a.city}* (${a.code})\n\n📅 *Fecha de salida*?`,ht:`✅ Destinasyon: *${a.city}* (${a.code})\n\n📅 *Dat depa*?`}),'segment_departure_date',{criteria:{destination:a.code,destination_city:a.city},destination_candidates:null},{normalized:a.code});}prompt(){return '1, 2, 3...';}}

import {DoniShell} from '@/components/DoniShell';import {InventoryManager} from '@/components/operations/InventoryManager';import {requirePageUser} from '@/lib/auth/session';import {hasRole} from '@/lib/auth/permissions';import {listInventory} from '@/services/operations/inventory';
export default async function Page(){const user=await requirePageUser('AGENT');const raw:any[]=await listInventory();const rows=raw.map(r=>({...r,adultPrice:String(r.adultPrice),childPrice:r.childPrice==null?null:String(r.childPrice),infantPrice:r.infantPrice==null?null:String(r.infantPrice)}));return <DoniShell title="Manual Inventory" active="/inventory" user={user}><InventoryManager rows={rows} canEdit={hasRole(user.role,'ADMIN')}/></DoniShell>}

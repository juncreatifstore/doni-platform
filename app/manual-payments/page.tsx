import {redirect} from 'next/navigation';import {DoniShell} from '@/components/DoniShell';import {ManualPaymentCenter} from '@/components/manual-payments/ManualPaymentCenter';import {requirePageUser} from '@/lib/auth/session';import {listManualPaymentReviews} from '@/services/payments/manual-review';import {canAccessManualPayments,filterManualPaymentRowsForUser} from '@/lib/auth/manual-payment-access';
export default async function Page(){const user=await requirePageUser('AGENT');if(!canAccessManualPayments(user))redirect('/overview?forbidden=1');const rows=filterManualPaymentRowsForUser(await listManualPaymentReviews(),user);const clean=JSON.parse(JSON.stringify(rows,(_k,v)=>typeof v==='object'&&v?.constructor?.name==='Decimal'?String(v):v));return <DoniShell title="Paiements manuels" active="/manual-payments" user={user}><ManualPaymentCenter initial={clean}/></DoniShell>}

import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {createMarketingSeason,listMarketingSeasons,marketingSeasonMeta,SEASON_STATUSES,SEASON_TYPES} from '@/lib/workspace/marketing-seasonality';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [items,meta]=await Promise.all([listMarketingSeasons(),marketingSeasonMeta()]);return NextResponse.json({success:true,items,...meta});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.title!=='string'||!b.title.trim())throw new Error('title_required');if(!SEASON_TYPES.includes(b.type))throw new Error('invalid_type');if(b.status&&!SEASON_STATUSES.includes(b.status))throw new Error('invalid_status');if(!b.startAt||Number.isNaN(new Date(b.startAt).getTime()))throw new Error('start_date_required');const item=await createMarketingSeason({...b,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

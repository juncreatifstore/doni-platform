import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {SEASON_STATUSES,SEASON_TYPES,updateMarketingSeason} from '@/lib/workspace/marketing-seasonality';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();if(b.type!==undefined&&!SEASON_TYPES.includes(b.type))throw new Error('invalid_type');if(b.status!==undefined&&!SEASON_STATUSES.includes(b.status))throw new Error('invalid_status');if(b.startAt!==undefined&&Number.isNaN(new Date(b.startAt).getTime()))throw new Error('invalid_start_date');const item=await updateMarketingSeason(id,b,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

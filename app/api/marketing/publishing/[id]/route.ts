import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {PUBLISHING_PLATFORMS,PUBLISHING_STATUSES,publishingGovernance,updateMarketingPublishing} from '@/lib/workspace/marketing-publishing';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();if(b.platform!==undefined&&!PUBLISHING_PLATFORMS.includes(b.platform))throw new Error('invalid_platform');if(b.status!==undefined&&!PUBLISHING_STATUSES.includes(b.status))throw new Error('invalid_status');const item=await updateMarketingPublishing(id,b,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item,governance:await publishingGovernance(item)});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

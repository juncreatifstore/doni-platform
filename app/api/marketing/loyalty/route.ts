import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {createMarketingLoyalty,listMarketingLoyalty,loyaltyMetrics,LOYALTY_STATUSES,LOYALTY_TIERS} from '@/lib/workspace/marketing-loyalty';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const items=await listMarketingLoyalty();return NextResponse.json({success:true,items,metrics:loyaltyMetrics(items)});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.customerName!=='string'||!b.customerName.trim())throw new Error('customer_name_required');if(b.tier&&!LOYALTY_TIERS.includes(b.tier))throw new Error('invalid_tier');if(b.status&&!LOYALTY_STATUSES.includes(b.status))throw new Error('invalid_status');const item=await createMarketingLoyalty({...b,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

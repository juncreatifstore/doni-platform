import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {MARKETING_PLAN_STATUSES,updateMarketingPlan} from '@/lib/workspace/marketing-planning';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();const patch:any={};for(const k of ['title','focus','theme','currency','specialAction','trainingTopic','reportNotes'] as const)if(typeof b[k]==='string'||b[k]===null)patch[k]=b[k];if(Array.isArray(b.objectives))patch.objectives=b.objectives.map(String);if(typeof b.budget==='number'||b.budget===null)patch.budget=b.budget;if(typeof b.status==='string'&&MARKETING_PLAN_STATUSES.includes(b.status))patch.status=b.status;const plan=await updateMarketingPlan(id,patch,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,plan});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

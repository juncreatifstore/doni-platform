import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {INSIGHT_PRIORITIES,INSIGHT_STATUSES,INSIGHT_TYPES,createMarketingInsight,listMarketingInsights,marketingInsightMeta} from '@/lib/workspace/marketing-insights';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [items,meta]=await Promise.all([listMarketingInsights(),marketingInsightMeta()]);return NextResponse.json({success:true,items,...meta});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.title!=='string'||!b.title.trim())throw new Error('title_required');if(typeof b.observation!=='string'||!b.observation.trim())throw new Error('observation_required');if(!INSIGHT_TYPES.includes(b.type))throw new Error('invalid_type');if(b.status&&!INSIGHT_STATUSES.includes(b.status))throw new Error('invalid_status');if(b.priority&&!INSIGHT_PRIORITIES.includes(b.priority))throw new Error('invalid_priority');const item=await createMarketingInsight({...b,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

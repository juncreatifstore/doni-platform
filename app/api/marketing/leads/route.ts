import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {canAccessDepartments,dataScopeForUser} from '@/lib/auth/data-scope';
import {createMarketingLead,getMarketingLeadForUser,listMarketingLeadsForUser,MARKETING_LEAD_STATUSES,MARKETING_SOURCES,marketingAssigneesForUser,updateMarketingLead} from '@/lib/workspace/marketing';
function allowed(user:any){return canAccessDepartments(user,['MARKETING']);}
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});if(!allowed(auth.user))return NextResponse.json({success:false,error:'forbidden'},{status:403});const [leads,users]=await Promise.all([listMarketingLeadsForUser(auth.user),marketingAssigneesForUser(auth.user)]);return NextResponse.json({success:true,leads,users});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});if(!allowed(auth.user))return NextResponse.json({success:false,error:'forbidden'},{status:403});const scope=dataScopeForUser(auth.user);if(scope.mode==='none')return NextResponse.json({success:false,error:'forbidden'},{status:403});try{const b=await req.json();if(typeof b.name!=='string'||!b.name.trim())throw new Error('name_required');if(!MARKETING_SOURCES.includes(b.source))throw new Error('invalid_source');const ownerId=b.ownerId?String(b.ownerId):null;if(ownerId){const users=await marketingAssigneesForUser(auth.user);if(!users.some(u=>u.id===ownerId))return NextResponse.json({success:false,error:'owner_forbidden'},{status:403});}const lead=await createMarketingLead({name:b.name,phone:b.phone,email:b.email,source:b.source,status:MARKETING_LEAD_STATUSES.includes(b.status)?b.status:'NEW',country:scope.mode==='country'?scope.country:null,destination:b.destination,budget:Number.isFinite(Number(b.budget))?Number(b.budget):null,currency:b.currency,travelDate:b.travelDate,ownerId,nextFollowUpAt:b.nextFollowUpAt,notes:b.notes,createdById:auth.user.id});return NextResponse.json({success:true,lead});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}
export async function PATCH(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});if(!allowed(auth.user))return NextResponse.json({success:false,error:'forbidden'},{status:403});try{const b=await req.json();if(typeof b.id!=='string')throw new Error('id_required');if(!await getMarketingLeadForUser(b.id,auth.user))return NextResponse.json({success:false,error:'not_found'},{status:404});const patch:Record<string,unknown>={};for(const k of ['name','phone','email','destination','currency','travelDate','nextFollowUpAt','notes'])if(typeof b[k]==='string'||b[k]===null)patch[k]=b[k];if(typeof b.ownerId==='string'||b.ownerId===null){if(b.ownerId){const users=await marketingAssigneesForUser(auth.user);if(!users.some(u=>u.id===b.ownerId))return NextResponse.json({success:false,error:'owner_forbidden'},{status:403});}patch.ownerId=b.ownerId;}if(typeof b.status==='string'&&MARKETING_LEAD_STATUSES.includes(b.status))patch.status=b.status;if(typeof b.source==='string'&&MARKETING_SOURCES.includes(b.source))patch.source=b.source;if(b.budget===null||Number.isFinite(Number(b.budget)))patch.budget=b.budget===null?null:Number(b.budget);const lead=await updateMarketingLead(b.id,patch as any,{userId:auth.user.id});return NextResponse.json({success:true,lead});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

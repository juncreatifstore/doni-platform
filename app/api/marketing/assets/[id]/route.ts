import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {ASSET_CATEGORIES,ASSET_STATUSES,ASSET_USAGE,assetGovernance,updateMarketingAsset} from '@/lib/workspace/marketing-assets';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();if(b.category!==undefined&&!ASSET_CATEGORIES.includes(b.category))throw new Error('invalid_category');if(b.status!==undefined&&!ASSET_STATUSES.includes(b.status))throw new Error('invalid_status');if(b.usage!==undefined&&!ASSET_USAGE.includes(b.usage))throw new Error('invalid_usage');const item=await updateMarketingAsset(id,b,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item:{...item,governance:assetGovernance(item)}});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

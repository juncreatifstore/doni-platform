import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {ASSET_CATEGORIES,ASSET_STATUSES,ASSET_USAGE,assetGovernance,createMarketingAsset,listMarketingAssets,marketingAssetMeta} from '@/lib/workspace/marketing-assets';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [assets,meta]=await Promise.all([listMarketingAssets(),marketingAssetMeta()]);const items=assets.map(item=>({...item,governance:assetGovernance(item)}));return NextResponse.json({success:true,items,...meta});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.title!=='string'||!b.title.trim())throw new Error('title_required');if(typeof b.url!=='string'||!b.url.trim())throw new Error('url_required');if(!ASSET_CATEGORIES.includes(b.category))throw new Error('invalid_category');if(b.status&&!ASSET_STATUSES.includes(b.status))throw new Error('invalid_status');if(b.usage&&!ASSET_USAGE.includes(b.usage))throw new Error('invalid_usage');const item=await createMarketingAsset({...b,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item:{...item,governance:assetGovernance(item)}});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

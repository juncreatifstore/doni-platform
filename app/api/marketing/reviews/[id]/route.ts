import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {REVIEW_SOURCES,REVIEW_STATUSES,reviewGovernance,updateMarketingReview} from '@/lib/workspace/marketing-reviews';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();if(b.source!==undefined&&!REVIEW_SOURCES.includes(b.source))throw new Error('invalid_source');if(b.status!==undefined&&!REVIEW_STATUSES.includes(b.status))throw new Error('invalid_status');const item=await updateMarketingReview(id,b,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item:{...item,governance:reviewGovernance(item)}});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

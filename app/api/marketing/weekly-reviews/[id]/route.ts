import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {updateMarketingWeeklyReview,WEEKLY_ACTION_STATUSES,WEEKLY_REVIEW_STATUSES} from '@/lib/workspace/marketing-weekly-reviews';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params,b=await req.json();if(b.status!==undefined&&!WEEKLY_REVIEW_STATUSES.includes(b.status))throw new Error('invalid_status');if(Array.isArray(b.actions)&&b.actions.some((x:any)=>x.status&&!WEEKLY_ACTION_STATUSES.includes(x.status)))throw new Error('invalid_action_status');const item=await updateMarketingWeeklyReview(id,b,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {PARTNERSHIP_STATUSES,PARTNERSHIP_TYPES,updateMarketingPartnership} from '@/lib/workspace/marketing-partnerships';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();const patch:any={};for(const f of ['name','contactName','phone','email','country','city','ownerId','currency','nextAction','nextActionAt','notes'])if(b[f]!==undefined)patch[f]=b[f];if(b.type!==undefined){if(!PARTNERSHIP_TYPES.includes(b.type))throw new Error('invalid_type');patch.type=b.type;}if(b.status!==undefined){if(!PARTNERSHIP_STATUSES.includes(b.status))throw new Error('invalid_status');patch.status=b.status;}if(b.estimatedValue!==undefined)patch.estimatedValue=Number(b.estimatedValue);const item=await updateMarketingPartnership(id,patch,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {createMarketingPartnership,listMarketingPartnerships,marketingPartnershipAssignees,PARTNERSHIP_STATUSES,PARTNERSHIP_TYPES} from '@/lib/workspace/marketing-partnerships';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [items,users]=await Promise.all([listMarketingPartnerships(),marketingPartnershipAssignees()]);return NextResponse.json({success:true,items,users});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.name!=='string'||!b.name.trim())throw new Error('name_required');if(!PARTNERSHIP_TYPES.includes(b.type))throw new Error('invalid_type');if(b.status&&!PARTNERSHIP_STATUSES.includes(b.status))throw new Error('invalid_status');const item=await createMarketingPartnership({...b,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

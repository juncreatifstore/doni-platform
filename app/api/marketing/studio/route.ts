import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {createMarketingStudioItem,listMarketingStudio,marketingStudioMeta,STUDIO_TYPES} from '@/lib/workspace/marketing-studio';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [items,meta]=await Promise.all([listMarketingStudio(),marketingStudioMeta()]);return NextResponse.json({success:true,items,...meta});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const b=await req.json();if(typeof b.title!=='string'||!b.title.trim())throw new Error('title_required');if(typeof b.type!=='string'||!STUDIO_TYPES.includes(b.type))throw new Error('invalid_type');const item=await createMarketingStudioItem({title:b.title,type:b.type,brief:typeof b.brief==='string'?b.brief:null,assetUrl:typeof b.assetUrl==='string'?b.assetUrl:null,campaignId:typeof b.campaignId==='string'?b.campaignId:null,contentId:typeof b.contentId==='string'?b.contentId:null,ownerId:typeof b.ownerId==='string'?b.ownerId:null,reviewerId:typeof b.reviewerId==='string'?b.reviewerId:null,dueAt:typeof b.dueAt==='string'?b.dueAt:null,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

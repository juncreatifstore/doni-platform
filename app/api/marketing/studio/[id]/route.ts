import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {STUDIO_STATUSES,STUDIO_TYPES,updateMarketingStudioItem} from '@/lib/workspace/marketing-studio';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();const patch:Record<string,unknown>={};if(typeof b.title==='string')patch.title=b.title;if(typeof b.type==='string'&&STUDIO_TYPES.includes(b.type))patch.type=b.type;if(typeof b.status==='string'&&STUDIO_STATUSES.includes(b.status))patch.status=b.status;if(typeof b.brief==='string'||b.brief===null)patch.brief=b.brief;if(typeof b.assetUrl==='string'||b.assetUrl===null)patch.assetUrl=b.assetUrl;if(typeof b.campaignId==='string'||b.campaignId===null)patch.campaignId=b.campaignId;if(typeof b.contentId==='string'||b.contentId===null)patch.contentId=b.contentId;if(typeof b.ownerId==='string'||b.ownerId===null)patch.ownerId=b.ownerId;if(typeof b.reviewerId==='string'||b.reviewerId===null)patch.reviewerId=b.reviewerId;if(typeof b.reviewNote==='string'||b.reviewNote===null)patch.reviewNote=b.reviewNote;if(typeof b.dueAt==='string'||b.dueAt===null)patch.dueAt=b.dueAt;const item=await updateMarketingStudioItem(id,patch as any,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,item});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

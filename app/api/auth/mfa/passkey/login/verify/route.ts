import {NextResponse} from 'next/server';
import {db} from '@/lib/db';
import {assertMfaMethodAllowed,completeMfaChallenge,recordMfaChallengeFailure} from '@/lib/auth/mfa';
import {verifyAuthentication} from '@/lib/auth/passkeys';
import {createPortalSession} from '@/lib/auth/session';
import {audit} from '@/lib/audit';

export async function POST(req:Request){try{const b=await req.json();const challenge=String(b?.challenge||'');if(!challenge||!b?.response)return NextResponse.json({success:false,error:'passkey_required'},{status:400});const current=await assertMfaMethodAllowed(challenge,'PASSKEY');try{const verified=await verifyAuthentication(req,challenge,current.userId,b.response);await completeMfaChallenge(challenge,current.userId);const user=await db.portalUser.findUnique({where:{id:current.userId}});if(!user||!user.active)return NextResponse.json({success:false,error:'invalid_user'},{status:401});await createPortalSession(user.id);await db.portalUser.update({where:{id:user.id},data:{lastLoginAt:new Date()}});await audit({userId:user.id,action:'AUTH_LOGIN_MFA',entity:'PortalUser',entityId:user.id,metadata:{method:'PASSKEY',credentialId:verified.credentialId}});return NextResponse.json({success:true,role:user.role});}catch(e){await recordMfaChallengeFailure(challenge).catch(()=>{});throw e;}}catch(e){const error=e instanceof Error?e.message:'passkey_authentication_failed';const status=['invalid_mfa_challenge','mfa_method_not_allowed','unknown_passkey','passkey_authentication_failed','invalid_passkey_challenge'].includes(error)?401:error==='mfa_challenge_locked'?429:400;return NextResponse.json({success:false,error},{status});}}

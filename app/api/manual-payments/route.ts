import {NextResponse} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {listManualPaymentReviews} from '@/services/payments/manual-review';import {canAccessManualPayments,filterManualPaymentRowsForUser} from '@/lib/auth/manual-payment-access';
export async function GET(){const a=await requireApiUser('AGENT');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});if(!canAccessManualPayments(a.user))return NextResponse.json({success:false,error:'forbidden_department'},{status:403});const rows=filterManualPaymentRowsForUser(await listManualPaymentReviews(),a.user);return NextResponse.json({success:true,rows});}

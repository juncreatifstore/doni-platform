import {NextRequest} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {receiptBytes} from '@/services/payments/manual-review';import {assertManualPaymentReviewAccess,canAccessManualPayments} from '@/lib/auth/manual-payment-access';
export const runtime='nodejs';
export async function GET(_req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiUser('AGENT');if(!a.ok)return Response.json({success:false,error:a.error},{status:a.status});if(!canAccessManualPayments(a.user))return Response.json({success:false,error:'forbidden_department'},{status:403});try{const {id}=await params;await assertManualPaymentReviewAccess(id,a.user);const r=await receiptBytes(id);return new Response(r.bytes,{status:200,headers:{'content-type':r.mime,'cache-control':'private, no-store','x-content-type-options':'nosniff'}});}catch(e){const m=e instanceof Error?e.message:'receipt_failed';return Response.json({success:false,error:m},{status:m.startsWith('forbidden_')?403:404});}}

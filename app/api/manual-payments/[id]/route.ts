import {NextRequest,NextResponse} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {decideManualPaymentReview,getManualPaymentReview} from '@/services/payments/manual-review';
export async function GET(_req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiUser('AGENT');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});const {id}=await params;const row=await getManualPaymentReview(id);return row?NextResponse.json({success:true,row}):NextResponse.json({success:false,error:'not_found'},{status:404});}
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiUser('AGENT');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});try{const {id}=await params;const b=await req.json();const action=String(b.action||'') as 'approve'|'reject'|'needs_info'|'reanalyze';if(!['approve','reject','needs_info','reanalyze'].includes(action))throw new Error('invalid_action');const row=await decideManualPaymentReview(id,action,a.user,String(b.notes||''));return NextResponse.json({success:true,row});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

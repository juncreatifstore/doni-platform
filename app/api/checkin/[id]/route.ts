import {NextRequest,NextResponse} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {updateCheckin} from '@/services/operations/checkin';import {canAccessDepartments} from '@/lib/auth/data-scope';
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiUser('AGENT');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});if(!canAccessDepartments(a.user,['CUSTOMER_SERVICE','FLIGHT_OPS','OPERATIONS','MANAGEMENT']))return NextResponse.json({success:false,error:'forbidden_department'},{status:403});try{const {id}=await params;return NextResponse.json({success:true,row:await updateCheckin(id,await req.json(),a.user)});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {audit} from '@/lib/audit';
import {SETTING_BY_KEY} from '@/lib/settings/catalog';
import {clearSetting,saveSetting,settingsSnapshot} from '@/lib/settings/service';
export const runtime='nodejs';
export async function GET(){const auth=await requireApiUser('ADMIN');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});return NextResponse.json({success:true,settings:await settingsSnapshot(),canEditSecrets:auth.user.role==='SUPER_ADMIN'});}
export async function PUT(req:Request){const auth=await requireApiUser('ADMIN');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const body=await req.json();const key=String(body.key||'');const def=SETTING_BY_KEY.get(key);if(!def)return NextResponse.json({success:false,error:'unknown_setting'},{status:400});if(def.type==='secret'&&auth.user.role!=='SUPER_ADMIN')return NextResponse.json({success:false,error:'super_admin_required_for_secrets'},{status:403});if(body.clear===true){if(def.type==='secret'&&auth.user.role!=='SUPER_ADMIN')return NextResponse.json({success:false,error:'forbidden'},{status:403});await clearSetting(key);await audit({userId:auth.user.id,action:'SETTING_CLEAR',entity:'AppSetting',entityId:key,metadata:{category:def.category,secret:def.type==='secret'}});return NextResponse.json({success:true});}await saveSetting(key,body.value,auth.user.id);await audit({userId:auth.user.id,action:'SETTING_UPDATE',entity:'AppSetting',entityId:key,metadata:{category:def.category,secret:def.type==='secret'}});return NextResponse.json({success:true});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'settings_update_failed'},{status:400});}}

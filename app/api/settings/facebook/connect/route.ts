import {randomUUID} from 'node:crypto';
import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {requireRecentStepUp} from '@/lib/auth/session-security';
import {db} from '@/lib/db';
import {getSetting} from '@/lib/settings/service';
export const runtime='nodejs';
export async function GET(req:Request){const auth=await requireApiUser('SUPER_ADMIN');if(!auth.ok)return NextResponse.redirect(new URL('/login',req.url));const step=await requireRecentStepUp();if(!step.ok){const u=new URL('/step-up',req.url);u.searchParams.set('returnTo','/api/settings/facebook/connect');return NextResponse.redirect(u);}const appId=await getSetting<string>('facebook.app_id');const version=await getSetting<string>('facebook.graph_version');if(!appId)return NextResponse.redirect(new URL('/settings?facebook_error=app_id_missing',req.url));const state=randomUUID();const expiresAt=new Date(Date.now()+10*60*1000).toISOString();await db.appSetting.create({data:{key:`facebook.oauth.state.${state}`,category:'Facebook OAuth',value:{state,userId:auth.user.id,expiresAt,mfaVerified:true} as any}});const redirectUri=new URL('/api/settings/facebook/callback',req.url).toString();const v=String(version||'v23.0').replace(/^\/+|\/+$/g,'');const url=new URL(`https://www.facebook.com/${v}/dialog/oauth`);url.searchParams.set('client_id',appId);url.searchParams.set('redirect_uri',redirectUri);url.searchParams.set('state',state);url.searchParams.set('scope','pages_show_list,pages_read_engagement,pages_manage_posts');url.searchParams.set('response_type','code');return NextResponse.redirect(url);}

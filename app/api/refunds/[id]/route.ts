import {NextRequest,NextResponse} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {actOnRefund} from '@/services/post-booking/refunds';
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const a=await requireApiUser('ADMIN');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});try{const {id}=await params,b=await req.json();return NextResponse.json({success:true,row:await actOnRefund(id,String(b.action||''),b,a.user)});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'refund_action_failed'},{status:400});}}

import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {createWorkspaceTask,listWorkspaceTasksForUser,taskAssigneesForUser,TASK_PRIORITIES} from '@/lib/workspace/tasks';
import {isDepartment} from '@/lib/auth/departments';
import {dataScopeForUser} from '@/lib/auth/data-scope';
export async function GET(){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const [tasks,users]=await Promise.all([listWorkspaceTasksForUser(auth.user,{includeDone:true}),taskAssigneesForUser(auth.user)]);return NextResponse.json({success:true,tasks,users,currentUserId:auth.user.id});}
export async function POST(req:Request){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});const scope=dataScopeForUser(auth.user);if(scope.mode==='none')return NextResponse.json({success:false,error:'forbidden'},{status:403});try{const b=await req.json();const title=String(b.title||'').trim();if(title.length<3)return NextResponse.json({success:false,error:'invalid_title'},{status:400});const department=String(b.department||auth.user.department||'OPERATIONS');if(!isDepartment(department))return NextResponse.json({success:false,error:'invalid_department'},{status:400});if((auth.user.orgRole==='SECTION_MANAGER'||auth.user.orgRole==='AGENT')&&department!==auth.user.department)return NextResponse.json({success:false,error:'department_forbidden'},{status:403});const priority=String(b.priority||'NORMAL');if(!(TASK_PRIORITIES as readonly string[]).includes(priority))return NextResponse.json({success:false,error:'invalid_priority'},{status:400});const assigneeId=b.assigneeId?String(b.assigneeId):null;if(assigneeId){const allowed=await taskAssigneesForUser(auth.user);if(!allowed.some(u=>u.id===assigneeId))return NextResponse.json({success:false,error:'assignee_forbidden'},{status:403});}const task=await createWorkspaceTask({title,description:b.description?String(b.description):null,department,country:scope.mode==='country'?scope.country:null,priority:priority as any,assigneeId,dueAt:b.dueAt?String(b.dueAt):null,entityType:b.entityType?String(b.entityType):null,entityId:b.entityId?String(b.entityId):null,createdById:auth.user.id,createdByName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,task});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'create_failed'},{status:400});}}

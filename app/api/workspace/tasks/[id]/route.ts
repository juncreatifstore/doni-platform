import {NextResponse} from 'next/server';
import {requireApiUser} from '@/lib/auth/session';
import {updateWorkspaceTask,TASK_PRIORITIES,TASK_STATUSES} from '@/lib/workspace/tasks';
import {isDepartment} from '@/lib/auth/departments';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;const b=await req.json();const patch:Record<string,unknown>={};if(typeof b.title==='string')patch.title=b.title.trim();if(typeof b.description==='string'||b.description===null)patch.description=b.description;if(typeof b.department==='string'){if(!isDepartment(b.department))return NextResponse.json({success:false,error:'invalid_department'},{status:400});patch.department=b.department;}if(typeof b.priority==='string'){if(!(TASK_PRIORITIES as readonly string[]).includes(b.priority))return NextResponse.json({success:false,error:'invalid_priority'},{status:400});patch.priority=b.priority;}if(typeof b.status==='string'){if(!(TASK_STATUSES as readonly string[]).includes(b.status))return NextResponse.json({success:false,error:'invalid_status'},{status:400});patch.status=b.status;}if(typeof b.assigneeId==='string'||b.assigneeId===null)patch.assigneeId=b.assigneeId;if(typeof b.dueAt==='string'||b.dueAt===null)patch.dueAt=b.dueAt;if(typeof b.entityType==='string'||b.entityType===null)patch.entityType=b.entityType;if(typeof b.entityId==='string'||b.entityId===null)patch.entityId=b.entityId;const task=await updateWorkspaceTask(id,patch as any,{userId:auth.user.id,userName:auth.user.fullName||auth.user.username});return NextResponse.json({success:true,task});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'update_failed'},{status:400});}}

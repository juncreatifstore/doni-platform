import {db} from '../../../../lib/db';export async function GET(req:Request){const secret=process.env.CRON_SECRET;if(!secret||req.headers.get('authorization')!==`Bearer ${secret}`)return Response.json({ok:false},{status:401});const now=new Date();const r=await db.payment.updateMany({where:{status:{in:['CREATED','PENDING']},expiresAt:{lt:now}},data:{status:'EXPIRED'}});return Response.json({ok:true,expired:r.count,at:now.toISOString()});}

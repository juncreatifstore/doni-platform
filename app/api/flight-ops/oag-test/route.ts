import {NextRequest,NextResponse} from 'next/server';import {requireApiUser} from '@/lib/auth/session';import {oagRouteSearch} from '@/services/flight-ops/oag';
export async function POST(req:NextRequest){const a=await requireApiUser('ADMIN');if(!a.ok)return NextResponse.json({success:false,error:a.error},{status:a.status});try{const b=await req.json();if(!b.origin||!b.destination||!/^\d{4}-\d{2}-\d{2}$/.test(String(b.date||'')))return NextResponse.json({success:false,error:'invalid_request'},{status:400});return NextResponse.json({success:true,result:await oagRouteSearch(String(b.origin),String(b.destination),String(b.date),25)});}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:'failed'},{status:500});}}

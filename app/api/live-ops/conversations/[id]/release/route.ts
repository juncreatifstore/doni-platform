import { NextResponse } from 'next/server';import { requireApiUser } from '@/lib/auth/session';import { releaseConversation } from '@/services/live-ops/service';
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const {id}=await params;return NextResponse.json({success:true,conversation:await releaseConversation(id,auth.user)});}catch(e){const m=e instanceof Error?e.message:'release_failed';return NextResponse.json({success:false,error:m},{status:m==='conversation_not_found'?404:409});}}

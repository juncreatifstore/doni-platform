import { NextResponse } from 'next/server';import { requireApiUser } from '@/lib/auth/session';import { sendAgentMessage } from '@/services/live-ops/service';
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiUser('AGENT');if(!auth.ok)return NextResponse.json({success:false,error:auth.error},{status:auth.status});try{const body=await req.json();const {id}=await params;return NextResponse.json({success:true,...await sendAgentMessage(id,auth.user,String(body?.text||''))});}catch(e){const m=e instanceof Error?e.message:'send_failed';return NextResponse.json({success:false,error:m},{status:m==='conversation_not_found'?404:409});}}

'use client';

import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
import {startAuthentication} from '@simplewebauthn/browser';

export default function LoginPage(){
 const router=useRouter();
 const[username,setUsername]=useState(''),[password,setPassword]=useState(''),[challenge,setChallenge]=useState(''),[code,setCode]=useState(''),[methods,setMethods]=useState<string[]>([]),[error,setError]=useState(''),[loading,setLoading]=useState(false);
 async function usePasskey(){if(!challenge||loading)return;setLoading(true);setError('');try{const o=await fetch('/api/auth/mfa/passkey/login/options',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({challenge})});const oj=await o.json();if(!o.ok||!oj.success)throw new Error(oj.error||'passkey_options_failed');const response=await startAuthentication({optionsJSON:oj.options});const v=await fetch('/api/auth/mfa/passkey/login/verify',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({challenge,response})});const j=await v.json();if(!v.ok||!j.success)throw new Error(j.error||'passkey_authentication_failed');router.replace('/overview');router.refresh();}catch(e){const c=e instanceof Error?e.message:'passkey_failed';setError(c.includes('NotAllowed')?'La Passkey a été annulée ou n’est pas disponible sur cet appareil.':'Validation de la Passkey impossible.');}finally{setLoading(false)}}
 async function onSubmit(e:FormEvent){e.preventDefault();setError('');setLoading(true);try{if(challenge){const res=await fetch('/api/auth/mfa/challenge',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({challenge,code})});const data=await res.json().catch(()=>({}));if(!res.ok||!data?.success){const c=String(data?.error||'mfa_failed');setError(c==='invalid_mfa_code'?'Code incorrect. Vérifie Google Authenticator ou utilise un code de récupération.':c==='mfa_challenge_locked'?'Trop de codes incorrects. Recommence la connexion.':'Validation MFA impossible.');if(c==='mfa_challenge_locked'){setChallenge('');setMethods([]);setCode('');}return;}router.replace('/overview');router.refresh();return;}const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username,password})});const data=await res.json().catch(()=>({}));if(!res.ok||!data?.success){const c=String(data?.error||'login_failed');setError(c==='invalid_credentials'?'Identifiant ou mot de passe incorrect.':c==='too_many_attempts'?'Trop de tentatives. Réessaie dans quelques minutes.':'Connexion impossible.');return;}if(data.mfaRequired&&data.challenge){setChallenge(String(data.challenge));setMethods(Array.isArray(data.methods)?data.methods:[]);setPassword('');return;}router.replace('/overview');router.refresh();}catch{setError('Connexion impossible.');}finally{setLoading(false)}}
 const hasCode=methods.includes('TOTP')||methods.includes('RECOVERY');
 return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'#f6f8fb'}}><section style={{width:'100%',maxWidth:420,background:'#fff',border:'1px solid #e7eaf0',borderRadius:18,padding:32,boxShadow:'0 18px 50px rgba(15,23,42,.08)'}}><div style={{display:'flex',alignItems:'center',gap:12,marginBottom:26}}><div style={{width:44,height:44,borderRadius:14,display:'grid',placeItems:'center',background:'#0f172a',color:'#fff',fontWeight:800,fontSize:20}}>D</div><div><div style={{fontSize:22,fontWeight:800,color:'#0f172a'}}>DONI</div><div style={{fontSize:13,color:'#64748b'}}>Travel Operations Portal</div></div></div><h1 style={{fontSize:28,lineHeight:1.1,margin:'0 0 8px',color:'#0f172a'}}>{challenge?'Vérification de sécurité':'Connexion'}</h1><p style={{margin:'0 0 24px',color:'#64748b'}}>{challenge?'Valide avec ta Passkey ou ton facteur de secours.':'Accède au portail sécurisé DONI.'}</p><form onSubmit={onSubmit} style={{display:'grid',gap:16}}>{!challenge?<><label style={{display:'grid',gap:7,fontSize:14,fontWeight:600,color:'#334155'}}>Identifiant<input autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)} required style={{width:'100%',boxSizing:'border-box',height:46,border:'1px solid #cbd5e1',borderRadius:10,padding:'0 13px',fontSize:16}}/></label><label style={{display:'grid',gap:7,fontSize:14,fontWeight:600,color:'#334155'}}>Mot de passe<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required style={{width:'100%',boxSizing:'border-box',height:46,border:'1px solid #cbd5e1',borderRadius:10,padding:'0 13px',fontSize:16}}/></label></>:<>{methods.includes('PASSKEY')?<button type="button" onClick={usePasskey} disabled={loading} style={{height:50,border:0,borderRadius:11,background:'#0f172a',color:'#fff',fontSize:16,fontWeight:700}}>Utiliser une Passkey / clé de sécurité</button>:null}{hasCode?<label style={{display:'grid',gap:7,fontSize:14,fontWeight:600,color:'#334155'}}>Code Google Authenticator ou récupération<input inputMode="text" autoComplete="one-time-code" value={code} onChange={e=>setCode(e.target.value)} required={!methods.includes('PASSKEY')} placeholder="123456" style={{width:'100%',boxSizing:'border-box',height:52,border:'1px solid #cbd5e1',borderRadius:10,padding:'0 13px',fontSize:20,textAlign:'center'}}/></label>:null}</>}{error?<div role="alert" style={{padding:'11px 12px',borderRadius:10,background:'#fef2f2',color:'#b91c1c',fontSize:14}}>{error}</div>:null}{!challenge||hasCode?<button type="submit" disabled={loading} style={{height:48,border:0,borderRadius:11,background:challenge?'#fff':'#0f172a',border:challenge?'1px solid #cbd5e1':'none',color:challenge?'#334155':'#fff',fontSize:16,fontWeight:700}}>{loading?'Validation…':challenge?'Valider le code':'Se connecter'}</button>:null}{challenge?<button type="button" onClick={()=>{setChallenge('');setMethods([]);setCode('');setError('')}} style={{height:42,border:'1px solid #cbd5e1',borderRadius:10,background:'#fff',color:'#334155',fontWeight:600}}>Recommencer la connexion</button>:null}</form></section></main>;
}

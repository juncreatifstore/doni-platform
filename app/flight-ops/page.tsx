import Link from 'next/link';import {DoniShell} from '@/components/DoniShell';import {IncidentCenter} from '@/components/operations/IncidentCenter';import {requirePageUser} from '@/lib/auth/session';import {flightOpsSummary} from '@/services/flight-ops/service';import {listIncidents} from '@/services/operations/incidents';
export default async function Page(){const user=await requirePageUser('AGENT');const[d,incidents]:any=await Promise.all([flightOpsSummary(),listIncidents()]);return <DoniShell title="Flight Ops" active="/flight-ops" user={user}><div className="grid"><div className="card"><div className="statLabel">Vols suivis</div><div className="statValue">{d.active}</div></div><div className="card"><div className="statLabel">Contrôles dus</div><div className="statValue">{d.due}</div></div><div className="card"><div className="statLabel">Vols retardés</div><div className="statValue">{d.delayed}</div></div><div className="card"><div className="statLabel">Alertes / Check-in</div><div className="statValue">{d.pendingAlerts} / {d.checkins}</div></div></div><div className="quickLinks"><Link className="btn" href="/checkin">✅ Check-in Center</Link><Link className="btn" href="/baggage">🧳 Baggage Services</Link><Link className="btn" href="/inventory">🗂️ Inventaire manuel</Link></div><h2 className="sectionTitle">Incidents</h2><IncidentCenter rows={incidents}/><h2 className="sectionTitle">Suivi actif</h2><div className="card tableWrap"><table className="table"><thead><tr><th>Vol</th><th>Route</th><th>Départ</th><th>Statut</th><th>Retard</th><th>Porte / Terminal</th><th>Prochain contrôle</th></tr></thead><tbody>{d.rows.map((r:any)=><tr key={r.id}><td><b>{r.airlineCode}{r.flightNumber}</b></td><td>{r.origin} → {r.destination}</td><td>{new Date(r.scheduledDeparture).toLocaleString('fr-FR')}</td><td><span className={`badge ${['cancelled','diverted'].includes(r.flightStatus)?'badBadge':r.flightStatus==='delayed'?'warn':'ok'}`}>{r.flightStatus}</span></td><td>{r.delayMinutes??0} min</td><td>{r.gate||'—'} / {r.terminal||'—'}</td><td>{r.nextCheckAt?new Date(r.nextCheckAt).toLocaleString('fr-FR'):'—'}</td></tr>)}</tbody></table>{!d.rows.length&&<div className="emptyState">Aucun vol actif à suivre.</div>}</div></DoniShell>}

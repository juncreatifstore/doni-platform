import {DoniShell} from '@/components/DoniShell';import {BaggageCenter} from '@/components/operations/BaggageCenter';import {requirePageUser} from '@/lib/auth/session';import {listBaggage} from '@/services/operations/baggage';import {db} from '@/lib/db';
export default async function Page(){const user=await requirePageUser('AGENT');const [raw,tracks]:[any[],any[]]=await Promise.all([listBaggage(),(db as any).flightTracking.findMany({where:{active:true},orderBy:{scheduledDeparture:'asc'},take:100})]);const rows=raw.map(r=>({...r,price:r.price==null?null:String(r.price)}));return <DoniShell title="Baggage Services" active="/baggage" user={user}><BaggageCenter rows={rows} tracks={tracks}/></DoniShell>}
